mod interval;
mod zone;
mod enriched_text;
use crate::interval::*;
use crate::zone::*;
use crate::enriched_text::*;


#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
